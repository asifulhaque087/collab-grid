package inventory

const (
	// DefaultWidgetSize mirrors DEFAULT_WIDGET_SIZE from the TS service.
	DefaultWidgetSize = 190

	maxCSVUploadSize   = 10 << 20 // 10 MB
	csvRequiredMessage = "A CSV file is required."
)
